#!/bin/sh
set -e
cd /srv/logstash
exec 2>&1
exec chpst -u logstash \
    java <%= @options[:java_opts] %> \
    -jar /usr/lib/logstash/logstash.jar \
    agent \
    -f /srv/logstash/<%= @options[:name] %>.conf.d
